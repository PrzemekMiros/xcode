---
title: Realizacje
description: Zobacz wybrane realizacje stron, sklepów i portali internetowych które wykonałem.
---

<section class="pt-medium pb-medium bg-light-1 radius-lg">
    <div class="container">
    <div class="row g-3 g-md-4">
    {% for post in collections.works %}
        <div class="col-md-6 mb-4">
           <div class="work-card radius-sm">
                <a href="{{ post.data.link }}" title="{{ post.data.title }}" target="_blank">
                {% workImage "src/" + post.data.thumbnail, post.data.title %}
                <div class="work-card-content p-4">
                    <p class="subheading my-0 bold color-light-3"> 
                        {{ post.data.client }}
                    </p>
                    <h3 class="mt-2 color-light-1">
                        {{ post.data.title }}
                    </h3>
                </div>
                </a>
           </div> 
        </div>
    {% endfor %}
    </div>
    </div>
</section>